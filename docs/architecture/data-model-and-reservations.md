# Data Model and Reservation Rules

Status: T3 schema and T6 reservation services implemented

## Tables

### users

- `id`: generated identity primary key
- `name`: required text
- `email`: required, normalized lowercase, unique
- `password`: required Argon2id hash, never plaintext
- `role`: required text constrained to `customer` or `admin`
- `created_at`: timestamp with time zone, generated by PostgreSQL

### hotels

- `id`: generated identity primary key
- `name`: required text
- `description`: required text
- `city`: required text with a normalized search index
- `address`: required text
- `rating`: numeric value constrained from 0.0 through 5.0
- `image_url`: required validated HTTP or HTTPS URL or first-party absolute asset path
- `is_active`: required boolean, defaults to true
- `created_at`: timestamp with time zone

### rooms

- `id`: generated identity primary key
- `hotel_id`: required foreign key to `hotels`
- `name`: required room-type name
- `description`: required text
- `price_per_night`: positive `numeric(10,2)`
- `capacity`: positive integer
- `total_rooms`: positive integer
- `is_active`: required boolean, defaults to true
- `created_at`: timestamp with time zone

One row represents a room type, not a physical numbered room.

### reservations

- `id`: generated identity primary key
- `user_id`: required foreign key to `users`
- `room_id`: required foreign key to `rooms`
- `check_in`: PostgreSQL `date`
- `check_out`: PostgreSQL `date`
- `guests`: positive integer
- `price_per_night_snapshot`: positive `numeric(10,2)` captured at booking time
- `total_price`: positive `numeric(18,2)` booking-time snapshot
- `status`: `confirmed` or `cancelled`
- `idempotency_key`: required UUID, unique per user
- `request_fingerprint`: required SHA-256 hexadecimal digest used to detect conflicting reuse of an idempotency key
- `created_at`: timestamp with time zone

## Date model

Stay dates use half-open intervals:

```text
[check_in, check_out)
```

The checkout date is not occupied. Existing and requested stays overlap when:

```text
existing.check_in < requested.check_out
AND
existing.check_out > requested.check_in
```

## Availability

For one room type and requested stay:

```text
remaining = total_rooms - overlapping confirmed reservations
available = remaining > 0
```

Guest capacity filters room types before inventory is counted.

## Reservation transaction

Reservation creation runs inside one PostgreSQL transaction:

1. Lock the authenticated user's row to serialize that user's reservation submissions.
2. Check whether the user already has a reservation for the supplied idempotency key. Return it when room, dates, and guest count match, or reject conflicting key reuse.
3. Lock the selected room-type row with `SELECT ... FOR UPDATE`.
4. Validate that the room is active and accommodates the guest count.
5. Recount overlapping `confirmed` reservations.
6. Reject the request when the count is at least `total_rooms`.
7. Calculate nights in PostgreSQL from the two dates.
8. Snapshot the locked nightly price and calculate the total from it.
9. Insert a `confirmed` reservation.
10. Commit.

The user-row lock makes duplicate submissions deterministic. The room-type lock serializes booking attempts for the same inventory. A concurrent request waits, then observes the reservation committed by the first request.

An identical retry with the same user and idempotency key returns the original reservation. Reusing that key with different room, date, or guest input returns a conflict. The fingerprint contains only normalized reservation input and does not contain credentials or personal information.

## Status transitions

Allowed transition:

```text
confirmed -> cancelled
```

`cancelled` is terminal. This keeps status restoration from silently overbooking inventory.

## Universal inventory-lock protocol

Any operation that can change availability must lock the same room-type row before it reads or changes inventory-related state. This includes:

- Reservation creation
- Confirmed-to-cancelled status changes
- `total_rooms` changes
- Room-type deactivation
- Hotel deactivation when it affects room types

Using one lock target gives these operations a shared queue. Without it, a booking can race an administrator edit even when each operation uses its own transaction.

## Administrative inventory edits

- `total_rooms` cannot be reduced below the maximum simultaneously occupied inventory across existing active reservations.
- Room types and hotels are deactivated by setting `is_active = false`, never deleted through the application API.
- Public search and availability queries exclude inactive hotels and room types.
- Historical reservations continue to resolve their original hotel and room-type data.
- Deactivation and inventory edits use the universal inventory-lock protocol.

Hotel deactivation locks its room rows in identifier order before changing active state. Stable lock order reduces deadlock risk when a hotel operation touches several room types.

## Money

Prices use PostgreSQL `numeric`, not JavaScript floating-point arithmetic. PostgreSQL calculates the authoritative total. The API serializes monetary values consistently as two-decimal strings.

## Migration ownership

- `backend/migrations/` is the authoritative schema history.
- Applied migration checksums are immutable. Corrections use a new numbered file.
- The runner serializes deployments with a PostgreSQL advisory lock and applies each migration transactionally.
- Foreign keys use `ON DELETE RESTRICT` so historical reservations cannot lose their user or room relationship.
- Search, inventory, overlap, and customer-history indexes match the planned query paths.
